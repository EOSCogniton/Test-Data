function index = get_data_index(data_struct,name)
    s = size(data_struct);
    n= s(2);
    index = NaN;
    for k=1:n
        if strcmp(data_struct(k).Name,name) %Compare si les dey
            index = k;
        end
    end
end